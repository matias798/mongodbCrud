<% include partials/header %>


<div class="container ">

  <!-- navbBar -->
  <nav class="navbar navbar-dark bg-dark d-flex justify-content-center rounded m-4">
    <a href="/" class="navbar-brand text-center">editar tarea mongoDb</a>
  </nav>
  <!-- /navbBar -->
    <div class="row m-2">
    <!--FORM-->
    <div class=" ">
      <div class="card">
        <div class="card-body ">
          <form action="/edit/<%= task._id %>" method="post">
            <div class="form-group ">
              <input class="form-control m-2" type="text" name="title" placeholder="Title" value="<%= task.title %>">
            </div>
            <div class="form-group">
              <textarea class="form-control m-2" name="description" cols="80"
                placeholder="Add a Description"
                ><%= task.description %></textarea>
            </div>

            <div class='d-flex justify-content-end'>
          <!-- Home button -->
            <a href='/' class="btn btn-success m-2">
              <i class="fas fa-home btn btn-success">Home</i>
          </a>
          <!-- /Home button -->
          
          <!-- update button -->
          <button class="btn btn-primary m-2" type="submit">
              Update 
          </button>
          <!-- update button -->
        </div>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>

<% include partials/footer%>